app = "ecom-alloy-production"
primary_region = "arn"

[build]
  dockerfile = "Dockerfile"

[env]
  ALLOY_APP_NAME = "ecom-alloy-production"
  ENV_NAME = "production"
  SCRAPE_TARGET_DNS = "ecom-fe-sveltekit-production.internal"
  SCRAPE_TARGET_APP = "ecom-fe-sveltekit-production"

[checks]
  [checks.ready]
    grace_period = "10s"
    interval = "30s"
    method = "GET"
    path = "/-/ready"
    port = 12345
    timeout = "5s"
    type = "http"

  [checks.healthy]
    grace_period = "10s"
    interval = "30s"
    method = "GET"
    path = "/-/healthy"
    port = 12345
    timeout = "5s"
    type = "http"

  [checks.metrics]
    grace_period = "10s"
    interval = "60s"
    method = "GET"
    path = "/metrics"
    port = 12345
    timeout = "5s"
    type = "http"

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1
